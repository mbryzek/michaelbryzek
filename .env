# Version stamp: release-sveltekit exports the real PUBLIC_VERSION /
# PUBLIC_RELEASED_AT into the build environment (process env beats .env files);
# these empty defaults just keep non-release builds compiling.
PUBLIC_VERSION=""
PUBLIC_RELEASED_AT=""
